1)подключение js библиотек
2)подключение сторонних css-стилей
3)jquery - валидация
4)regex - валидация 
5)адаптивная разметка (например, bootstrap)
6)jquery - ajax запросы
7)общая конфигурация веб-приложения, web.xml и context.xml
8)Maven - зависимости и плагины, библиотеки для приложения и библиотеки для контейнера
9)HttpServlet - как контроллер в MVC (для обработки форм с jsp страниц и для передачи данных на сохранение в модель)
10)JSP страницы как view для отображения данных и формы отправки
11)DAO - Java объект отвечающий за запросы по сохранению объекта в базу/получения из базы
12)JSTL - стандартные тэги, displaytag для таблиц
13)Properties файлы в Java, загрузка данных из них
14)Log4j - промышленный стандарт логгирования, настройка вывода лога определенного класса в консоль, в файл
15)авторизация пользователей (basic, form) и хранение сведений о пользователях (tomcat-users.xml, JDBC realm), MD5 хэш
16)JSF
17)Validation jquery plugin
18)POJO (Java объект с set/get и заключенной бизнес логикой, без подключения к базе данных)

Делал проект в эклипсе, maven подключил в самом конце. Некоторые библиотеки не подключались через зависимости и находятся по адресу \WebContent\WEB-INF\lib
Томкат также настраивался отдельно версия 7.0.72, конфиг будет в приложении.